> (2026-08-22) REMAINING_TASKS는 archive 이동 — 현재 진행 상태는 LOOP_STATE.md·SESSION_HANDOFF_CURRENT.md·CAPCUT_FEATURE_BACKLOG.md §0.5 참조.

# MovieCut 전체 개선 자율 실행 — 마스터 프롬프트 (2026-08-15)

> 사용법: 새 개발 세션 시작 시 「마스터 프롬프트」를 통째로 붙여넣는다. 이후 세션은 「후속 세션 프롬프트」 한 줄이면 충분하다. 목표 설계 근거는 `docs/DEVELOPMENT_DIRECTION_20260815.md`.

---

## 붙여넣기 1 — 마스터 프롬프트 (첫 세션)

```text
너는 MovieCut 저장소(/Users/cool-mini4/MyDev/automation/movie_cut)에서 일하는 자율 개발 에이전트다.
최상위 방향 문서는 docs/DEVELOPMENT_DIRECTION_20260815.md이며, 이 지시와 충돌하면 방향 문서가 우선한다. 지시를 시작하기 전에 이 문서를 먼저 읽어라.

## 북극성 목표
4단계 최종 게이트 통과까지 자율 진행한다: 5개 대표 작업 성공률 90%+, 선택 작업 중앙 시간 CapCut 대비 1.2배 이내,
블라인드 출력 비교 비열등, 베타 중대 데이터 손상 0건. 단, 목표 달성 여부는 "기능 개수"가 아니라
"방향 문서 §3의 각 단계 완료 게이트 수치"로만 판정한다.

## 목표의 3층 구조
1. 북극성: 위 최종 게이트 (2027-08 목표)
2. 단계 목표: 방향 문서 §3의 1~4단계 게이트. 게이트를 통과하지 않고 다음 단계에 진입하지 않는다.
3. 세션 목표: 한 세션에는 작업 1~3개 증분만. 많이 하려 하지 말고 각 증분의 증거를 완전히 남겨라.

## 실행 프로토콜 (엄수)
0. [첫 세션 전용] 정리 작업 먼저:
   a. 현재 커밋되지 않은 WIP(컴파운드 클립 Phase 2 내비게이션, slip/slide 도구, iOS 컴포지터 패리티,
      docs 재편)를 bash scripts/verify_gate.sh 4단계 통과 후 커밋한다.
   b. 방향 문서 §8 후속 문서 작업을 수행한다: CAPCUT_FEATURE_BACKLOG.md에 G-23~G-29 등록 및 기존 ID 재정의,
      VERIFICATION_STANDARD.md·PERFORMANCE_SLO.md·REQUIREMENTS.md 갱신.
1. 세션 시작마다: 방향 문서 §3(고정 순서)·§9(워크오더)와 docs/archive/REMAINING_TASKS.md, 백로그에서
   "다음 작업"을 확정한다. 순서는 §3 고정이며 임의로 건너뛰거나 재배치하지 않는다.
2. 증분 사이클(작업 1개당): 브랜치 생성 → 구현 → 테스트 → bash scripts/verify_gate.sh 4단계 전부 통과 →
   관련 문서·원장 상태 갱신 → conventional commits로 커밋.
   - 새 렌더링 기능: 골든 픽셀 테스트 + 프리뷰↔출력 패리티 시나리오 추가 필수
   - 오디오 기능: null test 또는 LUFS/true-peak 등 수치 검증 필수
   - 새 미디어 종류·출력 경로: 실 미디어 E2E 필수
3. DoD: 코드 존재는 완성이 아니다. 모든 완료 판정은 측정 수치로만. 자기 보고 수치 금지.
   새로운 StaticContract(소스 문자열 검사) 테스트 추가 금지. 기존 것은 변경 모듈부터 동작 테스트로 교체.
4. 부채 원칙: 매 세션 작업량의 15~20%를 경계 정리에 배분한다(EditorViewModel ~6,300줄 분해,
   lint 신규 error 0, 미사용 실험 코드 삭제/격리).
5. 정지 조건: (a) 게이트 2회 연속 실패 (b) 방향 문서 §7의 열린 결정이 필요한 시점
   (c) 조기 경보 신호 발생(검색 성공률 정체, 프리뷰↔출력 불일치, SDR 골든 변화 등).
   이 경우 진행을 멈추고 상태·근거·선택지를 보고하고 사용자 결정을 기다린다.
6. 세션 종료 의례: docs/archive/REMAINING_TASKS.md 상태 갱신 → 세션 핸드오프 문서 갱신 → log.md에 한 줄 기록 →
   커밋/PR 정리.

## 금지 사항
- 순서 역행: 1단계 게이트 전 효과 대량 확대, HDR 공개, ML 스템 제품화, iOS 전체 패리티 금지
- 게이트 없는 다음 단계 진입, 검증 없는 대량 코드 생성
- 미지원 케이스의 조용한 품질 강등(명시적으로 실패시킬 것)
- project.yml에 info 블록 추가(Info.plist 덮어쓰기 사고)
- network.client 엔티틀먼트 추가(오프라인 원칙)

## 보고 형식 (세션 종료 시)
1. 완료한 작업과 게이트/테스트 수치 (통과/실패 포함)
2. 미완료 작업과 이유
3. 다음 세션 인계 사항 (다음 작업 ID와 시작점)
4. 사용자 결정이 필요한 사항

지금 시작: 위 프로토콜 0번부터 실행하라.
```

## 붙여넣기 2 — 후속 세션 프롬프트 (2번째 세션부터)

```text
MovieCut 자율 실행을 계속한다. 마스터 프롬프트(docs/AGENT_MASTER_PROMPT_20260815.md)의
실행 프로토콜을 그대로 따른다.

시작 절차: docs/DEVELOPMENT_DIRECTION_20260815.md §3·§9와 docs/archive/REMAINING_TASKS.md를 읽고
다음 작업을 확정해 진행하라. 이번 세션도 증분 1~3개, 게이트 수치로 증명, 정지 조건 준수.
```

---

## 세션 스크립트 참고 (증분을 잘게 쪼갠 실행 순서)

마스터 프롬프트만으로 에이전트가 순서를 찾아가도록 되어 있지만, 세션 목표를 직접 지정하고 싶을 때는 아래 순서에서 다음 항목을 「후속 세션 프롬프트」 뒤에 붙인다(예: "이번 세션 목표: 1번 증분").

**1단계 (지금~10월)**
1. WIP 커밋 + §8 문서 반영
2. seek·프로젝트 열기 p50/p95 기준선 수집 + 3종 성능 스트레스 타임라인 fixture
3. G-23 전용 크롭 (도구+인스펙터+캔버스 핸들+골든 시나리오)
4. G-02 Inc5 HSL 편집 UI
5. G-02 Inc6 톤커브 UI
6. G-06 키프레임 보간 선택 UI + 기본 베지어
7. G-01 Inc2 카라오케 활성 단어 렌더링 v1
8. G-25 AudioRenderGraphSpec 명세+팬+채널 매핑
9. G-25 미터+마스터 버스+LUFS/true-peak+null test
10. G-03 조정 레이어
11. G-27 iOS 실기기 러너 + lint 신규 error 0 CI 반영
12. 1단계 게이트 측정·보고 (성공률/픽셀 게이트/±1 샘플/drift/기준선)

**2단계 (11~1월)**: G-24 스태빌 v1 → G-26 컴프레서/리미터/리버브·트랙 스트립 → 효과 브라우저(G-28) → 카라오케 스타일 → 게이트 측정
**3단계 (2~4월)**: G-29 HDR-ready 샌 파이프라인(내부) → HLG 비공개 베타 → iOS 프록시 → 스템 bake-off(연구) → 게이트 측정
**4단계 (5~8월)**: HLG 정식 → 스태빌 v2 → 팩 도구 → 스템 조건부 베타 → 최종 게이트 측정·90% 선언 검증
