# 루프 자기 개선 원장 (LOOP_IMPROVEMENTS)

> 2026-08-23 신설(사용자 승인). 회차 중 발견한 **프로세스 개선** 후보를 쌓는 원장이다.
> 루프 회차는 이 원장의 A류 항목을 자율 실행할 수 있다(B류는 보고만). 규칙의 원천은
> `AGENT_LOOP_PROMPT_20260816.md` "자기 개선 증분" 섹션.

## 류 정의

- **A류(자율 실행 가능)**: 게이트·측정 강건화 / 플래키 테스트 안정화 / 문서 정합성 수습 / 계측 스크립트 개선 / 경계 정리 리팩터. 전제 — 제품 스코프·권한 서열 문서 불변, verify_gate 통과, 증분 1개 = 커밋 1개.
- **B류(사용자 승인 필요)**: 루프 프롬프트·크론 프롬프트 수정 / 권한 서열 문서(방향 문서 §3 등) / 새 기능 스코프 / DECISIONS 계열.

판정이 애매하면 반드시 B류로 분류한다.

## 원장

| ID | 개선 내용 | 류 | 상태 | 완료 기준 |
|---|---|---|---|---|
| LI-001 | G-28 응답성 탐침 강건화 — 고부하 위양성(worst 1개 판정·1초 창 → 중앙값 판정·2초 창·로드 종료 샘플링 종료). **신설 당시 선례로 등록(이미 완료)** | A | **완료(2026-08-23, 995a8b3)** | 동일 부하 조건 전체 게이트 통과로 실증(LOOP_STATE 기록) |
| LI-002 | 측정 픽스처 설계 검토 항목 — 단색·대칭 콘텐츠는 방향(회전)·왜곡(비등방 스케일) 판별이 원리적으로 불가(BUG-07: 단색 픽스처로 "판별 불가" 등록 → 비대칭 재실측에서 실제 결함 확인). 색·위치 비대칭이 있는 픽스처가 게이트 측정에 들어가기 전 "방향·스케일 판별 가능성" 점검 | B | 등록(2026-08-25) | 회차 프롬프트 또는 VERIFICATION_STANDARD의 픽스처 설계 체크리스트에 반영(사용자 승인 후) |

## 이력

- 2026-08-23: 원장 신설(사용자 승인 증분). LI-001 선례 등록.
