상태: USER_WAITING(G-25 설계 문서 승인 대기)
현재 증분: 완료 = **G-06 키프레임 값-시간 그래프 + G-25 설계 문서 초안**(aa1ba11, 2026-08-17 세션 7 — KeyframeGraphMath[곡선 평가=렌더러 수학·모드별 폴리라인] + KeyframeGraphView[추가/이동/삭제·제스처당 undo 1-step·리스트와 동일 커맨드] + 그래프 수학 테스트 7종. G-25 문서 docs/AUDIO_RENDER_GRAPH_SPEC_20260817.md — 샘플 타임베이스·글로벌 latency 보상·미지원 노드 거부·AAC 사후 검사·null test 절차). 게이트: 1,170 tests / 파리티 16/16 / verify_gate PASS. **대기 사유: G-25 AudioRenderGraphSpec 설계 문서 승인 필요(Inc 7 착수 전제)** — 사용자는 문서 검토 후 승인 또는 수정 지시. 승인 대기 중 진행 가능 증분: 모션 트래킹 게이트(T2-R1)·G-01 Inc4 카라오케 후속·인스펙터 경계. 캐비앗: ui_regression 전 상태 캡처 실패 = 호스트 접근성(TCC) 권한 회귀(코드 무관) — 사용자 재부팅/시스템 설정>개인정보 보호>접근성에서 터미널 권한 재확인 후 `bash scripts/ui_regression.sh` 재검증 필요(DoD ④[G-06 UI 회귀] 미실시 기록).
이전 완료: 세션 6(배경제거 트리거 9ad85ce)·세션 5(T1/T2/T3 7fc13fd·G-01 Inc3 00c97cf)·세션 4(카라오케 aefbfa5·transport 3c1c2f2)·세션 1-3(색공간·경계·G-02 Inc5).
마지막 커밋: aa1ba11
추가 완료(세션 8, cb71be2): UI 캡처 하니스 P0 보강(open 실행+PID 조회+진단 분리, 3회 연속 4/4) — G-06 DoD ④ 실질 충족, 무창 재발 시 window.txt 로그로 분류.
추가 완료(세션 9, 16:05 자동화 회차): **무창 재발 근본 원인 확정 = 세션 잠금(진단 문서 §4-D, TCC 가설 기각)** — 잠긴 세션에선 WindowServer가 신규 앱에 창을 부여하지 않아 프로세스 생존+창 0(WINDOW_COUNT_0)으로 나타남. 실증: 전체 화면 캡처=비밀번호 다이얼로그, loginwindow 창 수=1(잠금 중, 해제 시 0). ui_capture.sh 프리플라이트에 SESSION_LOCKED·SCREENSAVER_ACTIVE 감지 추가(loginwindow 창 수 기반 — frontmost 쿼리는 잠금 중 부실값 반환['ZCode'] 폐기). 잠금 상태 라이브 검증: 4/4 상태 즉시 거부·앱 미실행·exit 1. 캐비앗 정정: 이전 캐비앗의 "TCC 권한 회귀" 추정은 기각됨 — ui_regression은 **화면 잠금 해제 상태에서만** 실행할 것(잠금 시 즉시·명시적 실패).
추가 완료(세션 9 후속, 18:09): 진단 프롬프트 반영 감사 기반 마이너 증분 — `open` 전환 이후 비어 있던 `App log:` 안내 수정: 실패 시 app log를 아티팩트로 채움(ps 스냅샷[생존·창 없음 vs 조기 종료 판별] + 앱 PID 통합 로그 꼬리 40줄, `/usr/bin/log` 절대 경로 — zsh `log` 내장 명령 그림자 현상 회피), LAUNCH_FAIL 경로 preexisting_pids 기록, 성공 시에도 메타의 app_log 참조 실재화. 진단 프롬프트 §7.5/§13-P0-6/§14 부분 반영.
추가 완료(세션 10, 18:32): **모션 트래킹 하니스 게이트(T2-R1 전제) 완성** — `MOVIECUT_UITEST_MOTION_TRACKING=1` 게이트(UITestHarness): 실제 trackMotion 경로(provider→SetClipPropertyCommand(.keyframes))를 고정 초기 rect(픽스처 해석 ground truth x=32/320,y=88/240,72x64,80px/s)로 구동, 샘플≥25·키프레임=샘플×2·midX 이동>0.35·posX 이동>80px·ProjectStore 저장/적재 라운드트립 전량 보존 검증, JSON 행동 덤프(`MOVIECUT_UITEST_MOTION_TRACKING_DUMP`). 전용 스크립트 `scripts/run_motion_tracking_gate.sh`(픽스처 SHA-256 검증 b7a9cb2e…·sandbox OFF 자체 빌드·open -W·180s 와치독·단언). **2회 연속 PASS + 동일 행동 데이터(samples=61 keyframes=122 roundtrip=122 midx_delta=0.478, elapsed 1.78/1.19s)** — 결정성 실증. 설계 선택: §4.4 C1+C2 하이브리드(기존 하니스 패턴=앱 내 오케스트레이터+실커맨드), C3의 provider 계층은 기존 IoU 테스트가 담당. 후속 증분 후보: 신규 프로세스 재오픈(BOOTSTRAP_PROJECT)·프리뷰↔출력 파리티(T2-R1)·T2-M 측정.
추가 완료(세션 11, 20:08): **모션 트래킹 신규 프로세스 재오픈 검증 완성** — 하니스에 `MOVIECUT_UITEST_MOTION_TRACKING_SAVE`(실제 수동 저장 경로 saveProject(to:)로 저장·saved=1 보고)·`MOVIECUT_UITEST_MOTION_TRACKING_REOPEN=1`(신규 프로세스에서 BOOTSTRAP_PROJECT→openProject 실제 재오픈 경로로 적재 후 첫 비디오 클립 positionX/Y 키프레임 유지 검증, 런치 경합 대비 10s 타임라인 폴링) 추가. 스크립트 2단계화: 1단계(추적→적용→라운드트립→저장) → 2단계(재오픈→키프레임 수 일치 단언). **실행 결과: 1단계 keyframes=122·saved=1 → 2단계 재오픈 keyframes=122(posX=61/posY=61) 전량 보존 PASS** — 프로세스 경계를 넘어 결정성 실증. 검증 문서 §4.3의 "프로젝트 저장→재오픈→키프레임 유지" 요건 충족. 후보: 프리뷰↔출력 파리티(T2-R1)·T2-M 측정.
추가 완료(세션 12, 20:19): **키프레임 프리뷰 트리거 결함 수정 + 모션 트래킹 파리티 시나리오(T2-R1 전제)** — 결함: 키프레임만 있는 클립(모션 트래킹 출력)이 Mac-프리뷰에서 애니메이션 무시(PlaybackEngine 트리거에 keyframes 누락 — 출력 트리거·메타데이터·iOS-출력[무조건 부착]엔 포함). 크롭(G-23 Inc1)·배경제거(9ad85ce)와 동일 계열 "출력에만 반영" 결함의 키프레임 버전. 1조건 패치(`|| !clipInstruction.keyframes.isEmpty`). 파리티 시나리오 #18 `motion_tracking`(IMPORT=moving_subject·MOTION_TRACKING=1·times 0.3/1.7): **MAD 0.00 PASS(R=G=B=0.0)** — 프리뷰↔출력 동일 적용 실증. 허위 통과 배제 A/B 실험: 트래킹 유무 프리뷰 프레임 2,848픽셀 변화(≈44px 시프트=이론값)로 키프레임 가시 반영 입증 + 출력 경로 기존 반영 사실과 결합. `PARITY_ONLY=<name>` 단일 시나리오 필터 추가(반복·트리아지 속도).
추가 완료(세션 13, 21:35): **T2-M 모션 트래킹 분석 측정 완성** — `MotionTrackingAnalysisProbe`(Core, 명시적 arm/takeAndReset·프레임별 상태[seed/tracked/lostObservation/decodeFailure]+소요ms·CompositorRenderProbe 패턴, 제품 코드 env 의존 0)로 provider 루프 계측. `MotionTrackingAnalysisPerfTests`(env `MOVIECUT_T2M=1` 옵트인, 기본 skip — verify_gate 시간 불변): RTF·총시간·프레임별 p50/p95/p99(seed 웜업 제외)·IoU mean/median/p10/min/final(ground truth를 Support/MotionTrackingGroundTruth.swift로 단일화·기존 IoU 테스트와 공유)·실패율·최대 연속 실패·phys_footprint 피크(50ms 모니터 태스크). `scripts/run_t2m_motion_tracking.sh`: 환경 스냅샷(OS/모델/열·전원)+`swift test -c release`+아티팩트 저장. **첫 실측(Release 5회)**: RTF 0.351±0.094·p50 19.75ms·p95 26.35ms·IoU mean 0.793(5회 완전 동일=결정적)·실패율 0·피크 63.2MB(1회차 +51.3MB=Vision 웜업). 가림 재획득은 전용 픽스처 필요(후속). 검증 문서 §3.2 T2-M 요건 충족(RTF·총처리·분위수·메모리·키프레임수·IoU 통계·실패율·반복 편차).
추가 완료(세션 14, 22:38): **가림 재획득 측정 완성 — 모션 트래킹 계열 전 계층 닫힘** — 신규 픽스처 `moving_subject_occluded_320x240_3s_30fps.mp4`(동일 박스·궤적 + 회색 벽 x=120..216 상위 합성, 접촉 t=0.2·완전 가림 t=[1.1,1.4]·완전 재등장 t≥2.3, SHA 2c697b48… 해시 게이트 포함, 기존 픽스처 바이트 무변경). Ground truth에 가림 커버리지 함수 추가(duration 파라미터화). `T2M_OCC` 측정(스위트 `.serialized` — 병렬 실행 시 전역 프로브 세션 상호 도용 발견·수정): **실측 — 가림 전 IoU 0.904 → 가림 중 0.0(하락 단언 통과) → 재등장 후 0.0·재획득 없음**: 순차 추적(VNTrackObjectRequest)은 완전 가림 후 스스로 재검출하지 못함을 실증. **파생 제품 후보(사용자 결정)**: 추적 손실 시 재검출 시드(예: 손실 구간 후 사용자 rect 또는 detector 재시작) — 측정 계층은 완료, 기능 여부는 별도 증분. 최종 수치: RTF 0.344±0.084·p50 19.73ms·p95 25.54ms·IoU 0.793(결정적)·fail_rate 0.
추가 완료(세션 15, 23:34): **EditorViewModel 인스펙터 경계 분해** — 경계 로드맵 4번째(timeline✓→selection✓→transport✓→**inspector✓**). 신규 `EditorViewModel+Inspector.swift`(150줄): 인스펙터 표면 메서드 20개 순수 이동(updateSelected* 16종[transform·opacity·volume·audioFade·playbackRate·speedRamp·opticalFlow·keyframes·transition·textContent·chromaKey·colorCorrection·colorGrade·mask·cropRect·reverse·effects] + autoEnhance·autoColorCorrect 2종). 본체 5,503→5,392줄(−111, 보류분 환입 포함 순이동). **보류 기록(순수 이동 규칙 준수)**: 9개 메서드는 private 저장 의존으로 본체 유지(applyEQPreset·updateSelectedEQBandGain[clipEQPresets]·toggleBackgroundRemoval[backgroundRemovedClipIds]·applyStyleTransfer[clipStyles·styleTransferIndex]·autoColor/Levels/Enhance+selectedClipThumbnailRGBA[scopeContext]·importExternalLUT[lutErrorDescription]) — 본체 말미 MARK 섹션으로 명시. updateSelectedStickerTransform[isStickerClip]·refreshScopes[scopeContext] 동일 사유. StaticContract 3건 경계 파일 반영 갱신(AudioFade·TextStyle→+Inspector 직접, Phase33→메인+경계 결합으로 UI마커 금지 강화). 1,173 테스트 통과. 잔여 경계: media → effects → audio(Inc 9 직전) → export.
갱신: 2026-08-17 23:34
