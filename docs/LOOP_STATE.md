현재 증분(2026-08-29 7차) = **STAB-04 1차 완료(메인 세션 수동 3연속 회차) — W 측정 양분화 + 실결함 3건 포착**: `run_w_smoke.sh` 개명·W_STRICT 하니스(세로 임포트·STT 필수 게이트·실덕킹 분석)·실길이 픽스처 생성기(say 실발화 60s·세로·5분 마스터·120BPM)·`run_w_acceptance.sh`(ffprobe 품질 검증·시간 예산·파킹 회수 정석). **게이트 즉시 포착 — 백로그 §1.15**: BUG-ACC-01(P1·ProRes/명시적 비트레이트에서 겹치는 오디오 길이 합산 — 2s+4s→6.000s·300+300→600s 실측, 프리셋 경로는 정상)·BUG-ACC-02(P1·autoDuckOtherAudio 실분석 60초 실발화에서 continuation 파킹 — BUG-CA12-01 계열 신규 재현)·BUG-ACC-03(P2·비트 마커 수율 60s 120BPM→4개). 부수: 하니스 ProRes 레이스 예산 90초 고정→STRICT에서 길이 비례 확장(고정 예산이 "게이트<실작업"의 사례였음)·스모크 러너 앱 파킹 회수 보강. 검증: 스모크 W 5/5·29/29(40s)·verify_gate 5/5. acceptance는 BUG-ACC-01/02 수정까지 w1/w4 RED 정직 유지.
현재 증분(2026-08-29 6차) = **STAB-03 완료(메인 세션 수동 회차) — iOS 제품 결함 4건**: ① stepFrame이 frameStepTick 발행 → 뷰가 0.25s 공산 임계값 우회 강제 시크(zero-tolerance — 스텝 ~0.033s가 표시만 바뀌고 프레임이 남던 결함) ② 루프/정지를 AVPlayerItemDidPlayToEndTime 알림으로 이관(periodic observer는 정확한 종료 샘플링 미보장) + observer 클로저 MainActor.assumeIsolated 격리 ③ security scope을 Task 내부에서 열고 닫기(외부 Files/iCloud URL의 조기 권한 상실) ④ fileExporter 성공 후 중복 saveProject 제거(정상 저장 후 2차 쓰기 실패 오류표시 방지). 검증: **iOS 전체 62테스트/15스위트 PASS(신규 엔드 핸들러·틱 단언 포함)·verify_gate 5/5.** 정직 기록: 틱→강제 시크·알림 등록의 뷰 배선은 유닛테스트 불가 영역 — 실기기/수동 확인 항목(G-27 연계).
현재 증분(2026-08-29 5차) = **STAB-02 완료(메인 세션 수동 회차) — Mac ExportEngine A/V 펌프 병렬화**: 태스크그룹 동시 pump(iOS RENDER-02 패턴 포팅) + 펌프 실패 시 reader/writer 즉시 해체(취소된 리더의 copyNextSampleBuffer→nil로 형제 continuation 재개 — 그룹 대기 교착 방지) + writer .cancelled→CancellationError + 활성 writer 세션 추적·cancelExport 확장(제품 경로 취소 배관). 실측: **W4 3회 연속(84,957B 동일) + W 전체 5/5 × 3회 연속(15/15 워크플로·29/29 스텝) + 고아 0** — **W 측정 분쟁 판정: 외부 4/5 재현 안 됨(STAB-01·02 후 3회 연속 충족). 구조적 교착 위험은 코드에서 제거.** verify_gate 5/5(Core 1,425/211·Mac/iOS 빌드·lint). 잔여: 취소 E2E 단위테스트(라이브 export 테스트 인프라 부재 — 후속 증분), UI E2E 임포트 테스트 실패는 부모 커밋 동일 실패로 선결함·환경 판정(MACUI-01 계열 의심 — 후속 관찰).
현재 증분(2026-08-29 3차) = **외부 리뷰 #2 병합 — 안정화 계획 등록(docs 전용)**: `STABILIZATION_PLAN_20260829.md` 신설(STAB-01~08 원장·Phase 0~2 종료기준·실행주체 분류) + 크론 프롬프트 우선순위 체인에 계획 문서 편입(LI-004 — 사용자 병합 지시 승인 근거). 리뷰 주장 전부 코드 대조 확인(순차 펌프·W 픽스처 축소·iOS 4건·watchdog 수습 순서 오류·파리티 통합 스킵·CI 제한·MetricKit 충돌·122커밋 미푸시). **W 측정 분쟁(외부 4/5 vs 내부 5/5)은 STAB-01·02 완료 후 3회 연속 측정으로 판정 — 그 전까지 W 보고에 측정 환경 병기. Phase 1 x/7 판정은 Phase 2 종료 시 재실측 대조까지 보류.** 기존 큐(경계 분해 잔여·G-29·블라인드 A/B)는 STAB 창구 후 재개.
현재 증분(2026-08-29 4차) = **STAB-01 완료 — watchdog 고아 sleep 재수습**: 양 스크립트(run_w_scenarios·run_longform_soak)에 내부 sleep PID 직접 기록·회수·사후 단언 패턴 적용(구 pkill -P는 subshell 사망 후 재부모화로 빈 결과였음). 실측: W 5/5 완주(내장 단언 5회) + **soak 2run 우연 완주 — run1 803.2s/1,476MB·run2 700.5s/1,080MB·성장 0%·결정성 9/9 GATE PASS(사용자 대기였던 2run 확증 달성·내장 단언 2회)** + 외부 pgrep 고아 0(자기오탭 제거 정밀 검사). verify_gate 5/5.
현재 증분(2026-08-29 8차) = **BUG-ACC-01 완료 — 조정 레이어 오디오 스트립 편입이 원인**(실측 이분법: 오디오 스트림만 6s·비디오 2s → 자기 압축으로 [2,6] 밀린 조정 클립이 carriesAudio 통과). 빌더 가드 + Core 유닛 + 스모크 w4 **6.000s→4.000s 양 경로** + 스모크 W 5/5·게이트 5/5. acceptance 러너 보강(증거 보존·탐침 재시도·app.log 캡처) + **BUG-ACC-04 등록**(5분 마스터 간헐 전면 실패 2/4·오류 무표면 — 다음 app.log 판정).
현재 증분(2026-08-29 9차) = **BUG-ACC-04 2차 조사 — 진단 실장·팽창원 실측**: nil 경로+패키지 형상 로깅 실장·재현 6회(실패 2) — 실패 run은 **graphAudio=600s·vide 0..600 팽창**(300+300 순차 재래), 통과 run 전부 300s(프로젝트 원형 결정적·조정 [300,600]은 BUG-ACC-01 가드로 무해). 간헐 변수 = BGM 배치 요동(또는 동등 audible-600) — 다음 실패 재현의 `pkg project clips`로 판정(로깅 실장 완료). 게이트 5/5.
현재 증분(2026-08-30 루프) = **BUG-ACC-05 3차 조사 — 공급 지연 가설 기각·렌더 쪽 판정 반전**: 프리롤 웜업(t−0.2·150/400ms)은 비결정적 부분 통과(2/3·1/3)라 원복, **플래그 없는 직접 복사가 t=2.5에서 성공·검정 반환(4/4 결정적)** — 해당 item time에 검정 버퍼 존재 = 렌더 산출 문제(또는 낡은 검정 태깅), hasNewPixelBuffer는 필수 방어 확인. 4차 = 컴포지터 요청 단위 계측(소스 버퍼 귀속+합성 휘도). 게이트 5/5·기준선 보존(53.31 재현).
현재 증분(2026-08-30 루프 2차) = **BUG-ACC-05 4차 — 발동 조건 판명**: 컴포지터 요청 0건=평문 경로(무관 판명)·opacity 미러 3/3 기각·병렬 WIP(플래그 제거) 인수 검증 5/5 기각·미커밋 원복·**결정적: t=2.5가 폴링 attempt 0에서 34ms 만에 검정 버퍼 "신규" 반환 — 모든 재시도 변형은 attempt 0 성공으로 발동 불발이었음**(시크 직후 소스 미프라임 상태 렌더 완결·재렌더 없음). 5차 방향: 빠른 성공 의심→어웨이-앤-백 재렌더/플레이 넛지. 게이트 5/5.
현재 증분(2026-08-30 루프 3차) = **BUG-ACC-05 수정 완료(5차 — 어웨이-앤-백 재렌더)**: 갭 직후 0.5s 창의 빠른 성공(<150ms)만 불신·재렌더(전범위 불신은 motion 회귀 0.05→4.33으로 기각 후 갭 한정 게이트 채택). normal_delete **12회 연속 t=2.5 MAD 1.65**·재렌더 발동 확인·W 스모크 5/5·게이트 5/5. motion/freeze 실패는 소유 판정(양쪽 3/3 동일)으로 STAB-05 기존 플래이크. 진단 계측 전부 제거.
현재 증분(2026-08-31 루프) = **STAB-05 3차 — cross-dissolve 재편입**: 스크립트 태생 스킵 해제·하니스 TRANSITION_TARGET=first 노브 신설·행업 소멸 확인(STAB-02 순차 펌프 병렬화의 부수 효과 실증)·**구조 검증형 3/3 결정적 통과**(t=0.5/2.6 MAD 1.39/3.36·duration 4.5s 백타이밍). 디졸브 창은 스테일 공급 클래스에 차단 — **BUG-ACC-06 등록**(격자 정렬 판별기·normal_delete 부하 민감성·BUG-ACC-05 갭 검출 슬롯 오판 수정 포함). 풀스윕: cross_dissolve 통과·motion/freeze/normal_delete(부하) 기존 플래이크. 게이트 5/5.
현재 증분(2026-08-31 루프 2차) = **STAB-06 로컬 완료(CI 분할 — 원격 검증 대기)**: iOS 잡 3분할(fast 4스위트·AV 2직렬 스텝 개별 타임아웃·커버리지 4+11=15 실증·실측 0.55/40.2/5.0s) + nightly W smoke 추가·cross-dissolve 스킵 주석 갱신 + **BUG-ACC-06 증거 §1.13 BUG-CA12-01 통합(격자 판별기·부하 민감성·재현 레시피)**. 게이트 5/5.
현재 증분(2026-08-31 루프 3차) = **STAB-08 완료 — LOOP_STATE 자동 생성**: 3게이트 이력 레코더(append-only JSON) + 생성기(최근 3회·재현성 `--check-reproducible` 2회 동일 실측) + `docs/LOOP_STATE_REPORT.md` 생성(외부 리뷰 #9 모순의 구조적 해법 — 서술식 LOOP_STATE 병존하나 측정 상태표는 생성물만). Part 9 재감사: M1~M4 정정 확인·**MC-07 갱신**(19시나리오 상시 게이트화·스테일 클래스 4시나리오 잔여 명시). 부수: 파리티 스크립트 `--export_mp4` 언더스코어 타입수정(전 시나리오 FAIL 유발 중이었음 — 계측 도구 결함), W smoke 레코더 workflow 파생 수정. 게이트 5/5.
현재 증분(2026-09-01 루프) = **CODEX-17 완료 — iOS 플레이헤드 트림 정규 매핑 전환**: 양 트림이 `ClipTrimMath.compute` 경유(Mac 패리티·assetDuration/최소길이 가드) + 플레이헤드 사전 가드 유지(compute 클램프 계약과 차등) + `trimClip` sourceRange 선택 파라미터. 신설 3종(2x 매핑 보존 실측·리버스 반대 엣지·밖 거부) — iOS 68/16 2회 연속 PASS·게이트 5/5. 부수: CODEX-08의 rotated-outgoing 전환 테스트 1회 플래이크(3회 재측 정상 — 후속 관찰).
현재 증분(2026-09-01 루프 2차) = **CODEX-18 완료 — fps 프리셋 원자 undo**: Core `SetProjectCanvasAndExportSettingsCommand` 신규 + iOS updateCanvasPreset·Mac applyExportPreset(동일 클래스·자기 리뷰 발견) 전환 — undo 1회 전체 왕복 실측(신설 2종)·iOS 71/17 PASS·게이트 5/5.
현재 증분(2026-09-01 루프 3차) = **CODEX-19 완료 — CreateTrackCommand z-index 정규화**: 충돌 시 max+1(커맨드=단일 초이크 포인트 원천 해법·양플랫폼 caller 무수정)·비충돌 명시 z 보존. 신설 4종(결함 시퀀스·undo 왕복 포함) — Core 1,433/213 PASS·게이트 5/5.
현재 증분(2026-09-01 루프 4차) = **CODEX-20 완료 — RemoveTrack 잠금 가드**: 제거 전 ensureTrackIsEditable(다른 커맨드와 동일 계약) — 잠긴 트랙 삭제 거부·undo 스택 무오염 실측(신설 3종) Core 1,437/214·게이트 5/5. 병렬 WIP(캔버스 오염 조사)와 파일 무겹침으로 병행 진행.
현재 증분(2026-09-02 루프) = **STAB-08 잔여 완료 — worst-MAD 캡처**: 파리티 레코더가 시나리오별 worst MAD를 이력 JSON에 기록(tee+PIPESTATUS — 비교기 요약 라인 파싱·NA 가드) → 리포트 표에 수치 표시. 실측: PARITY_ONLY 프로브 이력 worst_mad=3.36 기록·리포트 재현성 2회 동일. **verify_gate는 병렬 세션 활성 WIP로 판정 불가 상태**(연속 실패 2건 모두 타 세션이 9분 전 수정 중인 PreviewView.swift 정적 계약 2종·iOS 빌드 1회 일시 — 제 변경은 스크립트/docs뿐로 Swift 테스트 무영향).
다음 증분(우선순위): CODEX-04·07(실기기 검증 권장)·CODEX-21 회귀 관찰 → STAB-02 취소 E2E → CODEX-04·07(실기기 검증 권장)·CODEX-21 회귀 관찰 → STAB-02 취소 E2E·worst-MAD 캡처. **STAB-07 제안은 상신 완료(DECISIONS Q13 — 사용자 결정 대기)** → 소형(STAB-02 취소 E2E·CODEX P1 7건·STAB-08 worst-MAD 캡처). STAB-05 루트·STAB-06 원격 = 대기. — BUG-ACC-04 수정 완료(2026-08-29 밤 — 동률 UUID 코인플립 판정·조정 레이어 규칙)·러너 계측 3건 완료(2026-08-30 — 탐침 접두사 판명)·STAB-04 2차 w2·w4 3연속 PASS 달성(w1=TCC+CA12-01·w5=기능 대기 잔여) → STAB-06 CI 분할 → STAB-06 CI 분할(yaml) → STAB-08·STAB-07 + 잔여 소형: STAB-02 취소 E2E·CODEX 병행(**P1 7건 — 04·07·08·09·10·11·17**, 04는 기존 열거 누락 보강). STAB 창구 종료 후 복귀 큐는 STABILIZATION_PLAN §3 Phase 3+ 체크리스트(리뷰 권장 2~4단계 전항 원장화) 참조. 사용자 대기 병렬: 실기기 2종·MACUI-01/U-08 TCC·**W1 acceptance STT용 음성인식 TCC 승인**·STAB-07 결정.상태: RUN
현재 증분(2026-08-29 2차) = **분해 한계 해제 — 공유 헬퍼 승격 이동(+AnalysisSupport) + F-20 하이라이트 이동(+AutoHighlights)**: 본체 5,206줄·Core 1,425 PASS·게이트 5/5(8919f3c). private→internal 승격은 동일 타깯 가시성 확대만(스코프·공개 표면 불변 — A류 근거 문서화). 직전 증분(2026-08-29 1차) = 경계 분리 부채 증분 완료 — F-12R 텍스트 스타일 프리셋 순수 이동(EditorViewModel+TextStylePresets.swift·본체 5,443줄) + **분해 한계 발견**: 잔여 섹션 전부 공유 file-private 헬퍼(sourceClipAndAsset 15·timelineMapping 15·recordAnalysisResult 10 등)에 얽힘 → 추가 분해는 internal 승격 리팩터(별도 승인 대상). soak 2run은 환경 차단(voiceagent 3종 활성·로드 28+ — 사용자 일시중지 대기). Mac 48/48·게이트 5/5.
현재 증분: 완료 = **외부 리뷰(2026-08-28) 반영 — P0/P1/P2 전량 수정·W 워크플로 판정 전환** — ①W 판정을 워크플로 단위(필수 단계 전부+출력물)로 변경 + watchdog 고아 sleep 수습(6분 잔류 종료) → **W 5/5 워크플로 PASS 재실측(W4 ProRes 포함 — 리뷰 타임아웃은 watchdog 파이프류로 판정, 재현 안 됨)** ②iOS 트림 no-op 폐지(플레이헤드 기준 앞/뒤 트림 다이얼로그 — TrimClipCommand 실경로) ③iOS 출력 컨테이너를 planner 해석으로(mp4 기본·URL·fileType 동일 소스) ④캔버스 fps 선택이 export 설정에 동기화 ⑤텍스트 시트 배경색 저장. 검증: iOS 신규 5/5(컨테이너 mp4·트림·오류 경로·배경색·fps 동기)·verify_gate 5/5. **Phase 1 정직 판정: 확실 6/7 — W 5/5 워크플로 + PARITY-TOL-01(a) 해결로 핵심 파리티 18/18·스윕 13/13 MAD≤2.0 재실측. 잔여 1개 = 실기기 2종(사용자 대기)**. PARITY-TOL-01(a): 픽스처 캔버스 정합 1440x1080(solid_red·smptebars·moving_subject) 교체 — 허용치 무변경·ripple 코덱잡음은 평탄 bars로 해소·optical_flow는 컴포지터 양다리 상쇄 근거로 320 유지(1440은 240s 와치독 초과 2회 실측). 경쟁분석 낡은 iOS 행 5건(M1~M4·M3 출력) 정정.
현재 증분(최신) = **장편 soak 게이트 구축(리뷰 #6 부분 이행, 2026-08-28)** — `run_longform_soak.sh`(RSS 누수·A/V 싱크·결정성·조건 기록 게이트). 유효 실측: 30분 단일 실행 wall 872.6s·RSS 1,574MB·길이 정확·A/V Δ0. 2run 확증은 **환경 오염으로 연기**(사용자 voiceagent 어댑터 3종이 측정 중 기기 점유 + 디스크 98% — 아티팩트 2.3GB 정리로 11Gi 회복·사용자 프로세스 무손상). 직전 증분: iOS Phase-1 잔여 UI 완료(리뷰 #3 전항) — ①프레임 스텝(±1/fps·클램프·일시정지) ②루프 재생(토글+끝도달 시 0 재시작) ③트랙 관리 시트(추가·mute·lock·스와이프 삭제 — 실제 커맨드) ④프로젝트 열기/저장(fileImporter/fileExporter·MovieCutProjectDocument — ProjectStore 공유 코덱) ⑤출력 프리셋 시트(해상도 720p/1080p/4K·컨테이너). 부수: Core RemoveTrackCommand public화·ExportResolution displayName/CaseIterable. 검증: IOSPhase1SurfacesTests 6/6(스텝 수학·루프·트랙·저장/열기 왕복·손상 명시 오류·프리셋)·게이트 5/5. → 다음: 실기기 2종(사용자)·G-29·블라인드 A/B·장편 soak·EditorViewModel 경계 분리.
**기존 결함 기록**: 자체 측정 게이트 5/5(1,425+신규)·iOS 시뮬 55/55(리뷰수정 5 포함)·Mac 48/48·W 5/5 워크플로. 실기기 1/3. **병렬 세션 경합 주의**: 커밋 직전 git status/diff --stat 확인 유지.
**범위 외 발견(후속 관찰)**: ui_regression 골든 의도 드리프트(AX 복구 시)·VFR timestamp 매칭 편차(CA12 문서 §6)·EditorViewModel.swift 5,486줄 경계 분리 지속(리뷰 #7)·CODEX-01~20(백로그 §1.14 — 스택 PR #19~#23 Codex 봇 리뷰 파생, 2026-08-29 등록. **P1 6건**: 04 PhotosPicker·07 relink 프리뷰·08 혼합 회전 트랙·09 전환 초과 클립 드랍·10 블라인드 반대 집계·11 REPS 미집계(A류)·17 iOS 트림 비정규 매핑. STAB 증분과 소형 병행 처리 권장, 06·17은 우선).
대기 결정 사항: G-27 실기기 2종 연결+잠금 해제·MACUI-01+U-08 회귀 실측(TCC)·CA-07 가격(사용자 전용)·CA-11·CA-13·CA-16(P2)·**STAB-07 MetricKit 방침(제안 접수 후)**. **122커밋 push 완료(2026-08-29 — 스택 PR #19~#23 순서 병합, 원격 CI 관찰 창구 확보)**. PARITY-TOL-01은 **실행 승인 완료(2026-08-28 리뷰 권고안 (a))**.
마지막 커밋: 31b36e2
갱신: 2026-08-29 10:43
