# MovieCut 사용성 감사 V1 — 2026-07-14

> 기준: `USABILITY_BENCHMARK_STANDARD.md` v1.0 / 기준 브랜치: `feat/core-backend-expansion` / 감사 시작 HEAD: `dbbc172`
> 판정 규율: 코드 존재나 static contract만으로 완료 처리하지 않는다. 자동 판정은 behavioral test·actual app E2E·결과물 probe를 우선하고, 사람 완주 시간이 필요한 행은 전제 기능이 있을 때만 `[사용자 확인 대기]`로 둔다.

## 1. 측정·실사 근거

- 콜드 스타트 actual app E2E: `/tmp/moviecut_d_cold_e2e.log` — G-16 scrub `requested/playhead/playback=1.250/1.250/1.250`, G-17 clipboard timeline+14초 export, G-15 image/mixed export, ducking RMS, 9:16 platform preset, title template export가 모두 PASS하고 마지막 줄 `E2E check OK`.
- 감사용 behavioral 측정: `/tmp/moviecut_ub_v1_behavioral_pre.log` — `swift test --filter 'TimelineScrubBehavior|ClipboardCommand|AutoCutPlanner|AudioDucking|TransitionPixelProcessor|SubtitleDocument'`, **57 tests / 11 suites PASS**.
- 클릭수 계측 부재: `docs/UI_METRICS.md:42`가 U-08 Inc 3 click-count metric을 `[진행중]`으로 기록하며, `App/MovieCutMacUITests`/DEBUG 하니스에 UB-V3 전 동작 click log가 없다.
- 카드뉴스 코드 부재 검색: `rg 'CardDocument|CardPage|CardTemplateSet|CardMasterStyle|BrandKit|CardImageSet|CardVideoPlan|ScriptCardDraft|ProjectCreationMode|cardNews|Card News' App Sources Tests scripts` → **0 matches**. 기존 timeline building block을 카드 워크플로우 완료로 간주하지 않는다.

## 2. UB 16개 전수 채점표 — 개발 전 기준선

| ID | 판정 | 코드/측정 근거 | 판정 이유·남은 검증 |
|---|---|---|---|
| UB-V1 | [사용자 확인 대기] | import+export actual app PASS(`/tmp/moviecut_d_cold_e2e.log:21-28`), title 14종 export PASS(`:32`); 자동자막 실제 provider/command 경로 `App/MovieCutMac/EditorViewModel.swift:3443-3493`; split 단축키 `MovieCutMacApp.swift:165-170` | SC-V1의 import/cut/delete/자동자막/title/9:16 export 전제는 존재한다. **≤10분·막힘 0**은 신규 사용자 화면 녹화+타임스탬프 없이는 판정 불가. |
| UB-V2 | [사용자 확인 대기] | speed command `EditorViewModel.swift:2607-2612`, transition UI `MediaLibraryPanel.swift:619-628`, ducking actual export `moviecut_d_cold_e2e.log:38`, 9:16 preset `:39-41`; 관련 behavioral suite PASS | SC-V2의 cut/배속/전환/BGM/덕킹/export 전제는 존재한다. **≤5분** 인간 완주 측정 대기. |
| UB-V3 | 🟡 | 매우 빈번 단축키: play `MovieCutMacApp.swift:103-110`, split `:165-170`, delete `:189-203`; G-17 Cmd+C/X/V actual app E2E PASS. 반면 `docs/UI_METRICS.md:42`는 click metric 미완 | 일부 상한을 만족할 입력 경로는 있으나 §5 전 동작(텍스트/자막/export/전환/BGM 등)의 자동 click count와 표 대조가 없어 “표 준수”를 증명하지 못함. |
| UB-V4 | 🟡 | zero-tolerance seek `App/MovieCutMac/Playback/PlaybackEngine.swift:139`; scrub E2E 1-frame 동기화 `moviecut_d_cold_e2e.log:17-18`; repository `os_signpost`/playback-start latency 측정 0건 | 정확한 스크럽은 증명됐지만 **스크럽·슬라이더 ≤100ms, 재생 시작 ≤300ms** 측정 로그가 없다. |
| UB-V5 | ❌ | auto-cut은 silence-only 별도 경로 `EditorViewModel.swift:2990-3105`; 자막은 별도 transcription 경로 `:3443-3533`; actual app `auto edit/SC-V3` 하니스 0건 | 영상 1개→자동컷+자막 초안→확인→export를 한 동작 흐름으로 묶는 기능이 없어 SC-V3 **≤2분** 전제 미충족. |
| UB-V6 | 🟡 | Preview는 다음 행동 CTA 제공 `PreviewPanel.swift:385-435`; Media Library도 import/select 안내 제공 `MediaLibraryPanel.swift:1098-1152`. 실패는 status text 1줄만 표시 `ContentView.swift:361-375`; 표면 inventory/100% 측정 없음 | 빈 상태 일부는 충족하지만 모든 실패에 retry/다음 행동이 있는지 보장하지 못하며 U-04도 미구현. “표면 100%” 미달. |
| UB-C1 | ❌ | 카드 코드 검색 0건; 등록 명세 G-18 현재 상태 `CAPCUT_SURPASS_SPEC_20260703.md:721-724` | 카드 추가/복제/삭제/순서 변경 모델·UI 자체가 없어 각각 ≤2클릭 전제 없음. |
| UB-C2 | ❌ | 카드 코드 검색 0건; 기존 `TextTemplate.builtIn`은 timeline 단일 텍스트 템플릿(`MediaLibraryPanel.swift:1425-1427`) | cover/body/emphasis/closing 세트 템플릿 ≥10종과 선택→적용 ≤2클릭 경로 없음. |
| UB-C3 | ❌ | `CanvasPreset` building block은 있으나 `CardDocument`/normalized card layout 0건 | 카드 규격 1:1·4:5·9:16 전환과 텍스트·로고 상대 배치 보존을 수행할 카드 문서 전제 없음. |
| UB-C4 | ❌ | 카드 canvas/inline editor 검색 0건 | 카드 텍스트 더블클릭 1회 인라인 수정 경로 없음. |
| UB-C5 | ❌ | `CardMasterStyle`/master inheritance 검색 0건 | 폰트·색·로고 위치 1회 변경을 전 카드에 ≤3클릭으로 전파하는 모델/command/UI 없음. |
| UB-C6 | ❌ | `BrandKit` 검색 0건 | 로고·색·폰트 저장소와 새 프로젝트 ≤2클릭 적용 경로 없음. |
| UB-C7 | ❌ | card image set exporter/순번 writer 검색 0건 | 전 카드 PNG/JPG 일괄 export, 순번 파일명, 인스타 규격 probe 경로 없음. |
| UB-C8 | ❌ | card video planner/9:16 slideshow 검색 0건 | 카드당 duration+전환+BGM slot을 적용하는 원클릭 영상화 및 SC-C3 경로 없음. |
| UB-C9 | ❌ | `ScriptCardDraft`/paragraph distributor 검색 0건 | 대본 문단 분배·온디바이스 요약 전제 없음. |
| UB-C10 | ❌ | UB-C1~C9 핵심 경로 부재 | SC-C1/C2/C3를 시작할 수 없어 인간 시간 대기로 올릴 전제도 없다. |

### 기준선 요약

- ✅ **0** / 🟡 **3** / ❌ **11** / `[사용자 확인 대기]` **2** = 총 16.
- `[사용자 확인 대기]`: UB-V1(SC-V1 ≤10분·막힘 0), UB-V2(SC-V2 ≤5분).
- V1 최초 감사이므로 직전 감사 대비 델타는 없음. 이 표가 이후 사이클의 baseline이다.

## 3. 이번 사이클 선택

- Cycle 1 선택: **G-04 Inc 1 + Inc 2** — `FilmstripGenerator` 실제 시간축 프레임 생성 + `(assetID, zoomBucket)` 128MB `FilmstripCache`.
- Cycle 2 선택: **G-04 Inc 3** — Inc 1~2를 실제 macOS `TimelineView` 클립 배경에 viewport-aware lazy/cancellation/fallback 경로로 연결한다.
- 선택 근거: §D D-3 우선순위를 그대로 적용했다. G-17은 Core behavioral 6/6 및 actual app E2E/14초 ffprobe가 완료됐고, Cycle 1 종료 시 G-04의 첫 미완 증분은 Inc 3이었다.
- 경계: Inc 3만 완결한다. Inc 4 hover scrub, Inc 5 signpost/perf와 G-04 AC 성능·메모리 실측이 남으므로 **G-04/UB-V4/UB 완료를 선언하지 않는다**.

## 4. 개발 후 재판정

- 완료 증분: G-04 Inc 1 `FilmstripGenerator` + Inc 2 `FilmstripCache` + Inc 3 실제 `TimelineView` consumer. `FilmstripViewportPlanner`가 named horizontal viewport 안 clip frame을 실제 scroll viewport 폭과 교차해 반 화면씩 prefetch하고 offscreen은 nil로 닫는다. MainActor `TimelineFilmstripStore`는 최대 32 active entry와 128MB/256-key cache를 사용하며 request identity 변경·view disappear에서 Task를 취소하고 generation token으로 stale publish를 거부한다. 기존 단일 썸네일은 async 준비·실패·취소 중 계속 렌더한다.
- focused behavioral: `/tmp/moviecut_d_c2_g04_inc3_focused.log` — `swift test --filter Filmstrip`, **11 tests / 1 suite PASS**. horizontal scroll 가시 범위, 양끝 clamp, offscreen nil, zoom identity, stale reject, cancellation/failure fallback을 실행 검증했다.
- actual app E2E: `/tmp/moviecut_d_c2_g04_inc3_e2e.log` — 기존 Inc 1~2 generator/cache PASS를 보존하고, 실제 `TimelineFilmstripLayer`/store/UI consumer가 `frames=32`, `distinct_digests=32`, `distinct_times=32`, `visible_span=21.600/30.000`, `visible_count=32/67`, `offscreen_skipped=1`, `cancelled=1`, `stale_rejected=1`, `fallback_before_ready=1`, `fallback_after_cancel=1`을 기록했다. 전체 스크립트는 `E2E check OK`로 종료했다.
- 개발 후 판정: ✅ **0** / 🟡 **3** / ❌ **11** / `[사용자 확인 대기]` **2**. 개발 전 대비 **델타 0/0/0/0**이다. Inc 3은 단일 썸네일 반복의 실제 UI 소비 격차를 해소했지만 UB-V4의 스크럽·슬라이더 ≤100ms/재생 시작 ≤300ms를 측정하지 않았고, G-04 Inc 4 hover와 Inc 5 signpost/perf도 남아 어떤 UB 행도 상향하지 않는다.
- `[진행중] 다음 증분: Inc 4, 시작점: App/MovieCutMac/TimelineView.swift:975`

## 5. 남은 미달 목록 (개발 후)

- 영상: G-04 Inc 4 hover, Inc 5 latency/perf·메모리 instrumentation 및 AC3 4단계 UI 밀도/AC5 비디오 외 시각 회귀 측정; UB-V3 click counter/전 동작 계측, UB-V5 통합 원클릭 auto-edit, UB-V6 surface inventory+actionable failure UI.
- 카드: UB-C1~C10 전부. 개발 순서는 §D D-3에 따라 G-04 잔여 후 G-18→G-19→G-21→G-20→U-10→G-22.
- 사람 확인: UB-V1/UB-V2. 카드 SC-C1/C2/C3/C4는 기능 전제가 없어 아직 `[사용자 확인 대기]`가 아니라 ❌다.
